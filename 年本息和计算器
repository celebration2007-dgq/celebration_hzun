#include <stdio.h>                                  //这是编译预处理指令 
int main()                                          //定义主函数
{                                                   //函数开始 
	float p0,r1,r2,r3,p1,p2,p3;                     //设置p0,r1,r2,r3,p1,p2,p3为单精度浮点型函数 
	scanf("%f",&p0);                                //输入本金 
	printf("本金为%.2f元\n",p0);                       //输出本金 
	printf("请输入活期利率\n");                     //提示用户输入利率 
	scanf("%f",&r1);                                //将r1设为利率1 
	p1=p0*(1+r1);                                   //计算结果 
	printf("一年后本息和为%f\n",p1);                //输出结果 
    printf("请输入定期利率\n");                     //提示用户输入利率
	scanf("%f",&r2);                                //将r2设为利率2 
	p2=p0*(1+r2);                                   //计算结果 
	printf("一年后本息和为%f\n",p2);                //输出结果 
	printf("请输入存两次半年定期的利率\n");         //提示用户输入利率 
	scanf("%f",&r3);                                //将r2设为利率2
	p3=p0*(1+r3/2)*(1+r3/2);                        //计算结果
	printf("一年后本息和为%f\n",p3);                //输出结果 
	getchar();                                	   //防止程序自动结束
	getchar();                                     //防止程序自动结束 
	return 0;                                      //使函数值返回0
		
 } 
